import { makeStyles } from "@material-ui/core";

export const useCommonStyles = makeStyles(() => ({
    centerItems: {
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
    },
}));

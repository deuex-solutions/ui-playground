import { makeStyles } from "@material-ui/core";

export const useFileReaderStyles = makeStyles(() => ({
    fileReaderRoot: {
        width: "100%",
        minHeight: "90vh",
        margin: "1rem 0",
    },
}));
